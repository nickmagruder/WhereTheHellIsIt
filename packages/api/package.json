{
  "name": "@wthi/api",
  "version": "0.0.1",
  "private": true,
  "description": "AppSync resolver Lambda + business logic — RBAC middleware, resolvers, photo service",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint src --ext .ts",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.758.0",
    "@aws-sdk/client-s3": "^3.758.0",
    "@aws-sdk/s3-request-presigner": "^3.758.0",
    "@aws-sdk/cloudfront-signer": "^3.758.0",
    "zod": "^3.24.1"
  }
}
